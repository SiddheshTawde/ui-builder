"use client";

import { ScrollArea } from "@root/components/ui/scroll-area";

export default function AddNewPage() {
  return (
    <ScrollArea className="container mx-auto h-[calc(100vh-64px)]">
    </ScrollArea>
  );
}
