{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev:all": "turbo dev",
    "dev": "turbo dev --filter=main",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "clean": "turbo run clean && rm -rf node_modules && rm -rf .turbo && rm -rf .next && rm -rf package-lock.json",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "turbo": "^2.0.6"
  },
  "engines": {
    "node": ">=18"
  },
  "name": "ui-builder",
  "packageManager": "npm@10.7.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
